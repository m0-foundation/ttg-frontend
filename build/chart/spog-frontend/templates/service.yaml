apiVersion: v1
kind: Service
metadata:
  name: {{ include "spog-frontend.fullname" . }}
  labels:
    {{- include "spog-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spog-frontend.selectorLabels" . | nindent 4 }}
